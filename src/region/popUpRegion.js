import React from "react";

export const popUpRegion = () => (
    <div className="container-fluid overflow-auto"
         style={{
             height: "500px",
             overflowY: "scroll",
             overflow: "scroll",
             scrollBehavior: "smooth",
             scrollbarColor: "green"
         }}>
    </div>
);